# VPS Optimization Script for Debian/Ubuntu

Комплексный скрипт оптимизации VPS серверов на базе Debian/Ubuntu с XanMod kernel, Zsh + Starship, Go, Node.js/NVM, Docker и усиленной безопасностью.

**🆕 Версия 2.0.0** - Добавлены Go и Node.js/NVM, исправлены все известные проблемы с Docker/Debian 13, полная оптимизация.
**📦 Версия 1.0.0** - Базовая VPS оптимизация (архив)

## 🎯 Ключевые особенности

### 🚀 XanMod Kernel
- **Автоопределение архитектуры** - x64v1, x64v2, x64v3, x64v4
- **Производительность** - оптимизация для low-latency и gaming
- **Совместимость** - поддержка современного оборудования
- **Безопасность** - последние патчи и обновления

### 🛠️ Современный Shell (Zsh + Starship)
- **Zsh** - мощный и настраиваемый shell
- **Starship prompt** - красивый и информативный промпт
- **Плагины Zsh** - автодополнения, подсветка синтаксиса
- **Алиасы** - удобные сокращения для частых команд

### 🐹 Go язык программирования
- **Последняя версия** - автоматическое определение и установка
- **SHA256 верификация** - безопасная загрузка
- **Архитектура** - автоопределение (x86_64, ARM и т.д.)
- **PATH конфигурация** - сразу готово к использованию

### ⬢ Node.js с NVM
- **Node Version Manager** - управление версиями Node.js
- **LTS версия** - автоматическая установка последней LTS
- **npm без sudo** - безопасная установка пакетов
- **Переключение версий** - `nvm use X.X` для быстрого переключения

### 🐳 Контейнеризация
- **Docker CE** - последняя версия с Docker Compose
- **Оптимизация daemon.json** - максимальная производительность
- **BuildKit** - ускорение сборки образов
- **Безопасность** - изолированная среда выполнения

### 🔒 Безопасность
- **SSH hardening** - отключение root, только ключи
- **Fail2Ban** - защита от брутфорс атак
- **UFW Firewall** - настройка файрвола
- **Автообновления** - безопасность на автопилоте

### ⚡ Производительность
- **BBR congestion control** - оптимизация сети
- **I/O планировщики** - оптимизация для SSD/NVMe
- **Управление памятью** - swappiness, кэширование
- **Лимиты системы** - увеличение файловых дескрипторов

## 📋 Системные требования

- **ОС:** Debian 11/12/13, Ubuntu 20.04+
- **Архитектура:** x86_64 (для XanMod)
- **Права:** root/sudo доступ
- **Память:** Минимум 2GB RAM (рекомендуется 4GB+)
- **Хранилище:** Минимум 10GB свободного места
- **Сеть:** Интернет соединение для загрузки пакетов

## 🛠️ Установка и использование

### Быстрый запуск
```bash
# Быстрый запуск (bash + curl)
bash <(curl -s https://raw.githubusercontent.com/snaplyze/optimize-linux/refs/heads/main/optimize_vps.sh)

# Или скачайте и запустите
wget https://raw.githubusercontent.com/snaplyze/optimize-linux/refs/heads/main/optimize_vps.sh
chmod +x optimize_vps.sh
sudo ./optimize_vps.sh
```

### Подготовка системы
```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Проверьте архитектуру
uname -m
# Должно быть x86_64

# Проверьте версию ОС
lsb_release -a
```

## 🎯 Компоненты оптимизации

### Системная конфигурация
- ✅ Обновление системы до последней версии
- ✅ Удаление ненужных пакетов
- ✅ Настройка репозиториев
- ✅ Создание пользователя с sudo
- ✅ Настройка локалей (ru_RU, en_US)
- ✅ Настройка часового пояса

### Ядро и производительность
- ✅ XanMod kernel (выбор версии)
- ✅ Оптимизация sysctl (BBR, лимиты)
- ✅ I/O планировщики (SSD/NVMe)
- ✅ Управление памятью (swappiness)
- ✅ Увеличение лимитов файлов

### Безопасность
- ✅ SSH hardening (только ключи, отключение root)
- ✅ Fail2Ban настройка
- ✅ UFW firewall конфигурация
- ✅ Автоматические обновления безопасности
- ✅ Настройка логирования

### Разработка
- ✅ Zsh + Starship конфигурация
- ✅ Go (последняя версия с SHA256 верификацией)
- ✅ Node.js + NVM (LTS с управлением версиями)
- ✅ Базовые утилиты (git, curl, htop, fzf, bat, cmake, gcc)
- ✅ SSH agent конфигурация
- ✅ Настройка окружения

### Контейнеризация
- ✅ Docker CE установка
- ✅ Docker Compose plugin
- ✅ Оптимизация daemon.json
- ✅ Настройка сетей и пулов адресов

## 🎨 Zsh + Starship особенности

### Zsh плагины
- **zsh-autosuggestions** - подсказки команд
- **zsh-syntax-highlighting** - подсветка синтаксиса
- **zsh-completions** - расширенные автодополнения

### Starship prompt
- 🌱 Git статус и ветка
- 🐋 Docker контекст
- 🐍 Python, ⬢ Node.js, 🐹 Go, 🦀 Rust версии
- ⏰ Время выполнения команд
- 💾 Использование памяти и диска

### Алиасы
```bash
# Docker
d='docker'
dc='docker compose'
dps='docker ps'
dcl='docker compose logs -f'

# Git
g='git'
gs='git status'
gcm='git commit -m'
gl='git log --oneline --graph'

# Система
ll='ls -alFh'
update='sudo apt update && sudo apt upgrade -y'
meminfo='free -h'
diskinfo='df -h'
```

## 🐹 Go программирование

### Установка
- Автоматическое определение последней версии с go.dev
- SHA256 верификация для безопасности
- Установка в `/usr/local/go`
- Автоматическая конфигурация PATH

### Использование
```bash
# Go сразу доступен
go version
go mod init example.com/hello
go run main.go

# Переменные окружения
echo $GOPATH     # $HOME/go
echo $GOROOT     # /usr/local/go
```

### Конфигурация
- `/etc/profile.d/golang.sh` - глобальная конфигурация для всех shells
- `~/.zshrc` - локальная конфигурация для Zsh (автоматически добавляется)

## ⬢ Node.js с NVM

### NVM установка
- Node Version Manager (NVM) v0.39.7
- Установка для root и регулярного пользователя
- Автоинициализация при запуске Zsh

### Node.js версии
- Автоматическая установка последней LTS версии
- Простое переключение между версиями: `nvm use 18`, `nvm use 20`
- npm install работает БЕЗ sudo

### Использование
```bash
# Проверка версии
node --version
npm --version

# Управление версиями
nvm list                    # Список установленных версий
nvm install 18             # Установить конкретную версию
nvm use 18                 # Переключиться на версию 18
nvm alias default 18       # Установить версию по умолчанию

# npm без sudo
npm install -g some-package   # Работает без sudo для пользователя!
npm install                    # Локальная установка в проект
```

### NVM конфигурация
```bash
# ~/.zshrc автоматически содержит:
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"
```

## 🚀 XanMod Kernel

### Версии ядра
- **x64v1** - Generic (рекомендуется для большинства)
- **x64v2** - Hardware Enablement
- **x64v3** - Real-time (low-latency)
- **x64v4** - Hardware Enablement + RT

### Преимущества XanMod
- Улучшенная производительность
- Поддержка нового оборудования
- Low-latency оптимизации
- Gaming оптимизации
- Регулярные обновления

### Установка
```bash
# Автоматический выбор версии
./optimize_vps.sh

# Ручная проверка
uname -r
# Должно показать XanMod ядро
```

## 🐳 Docker оптимизация

### daemon.json конфигурация
```json
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "storage-driver": "overlay2",
  "features": {
    "buildkit": true
  }
}
```

**Примечание:** Конфигурация оптимизирована для Debian 13 с поддержкой systemd socket activation. Не используются `default-address-pools` и `default-ulimits` для избежания конфликтов с systemd.

### Сеть и производительность
- **overlay2 storage driver** - максимальная производительность и совместимость
- **json-file логирование** - ограничение размера логов предотвращает переполнение диска
- **BuildKit** - ускорение сборки образов
- **systemd socket activation** - правильная интеграция с systemd для автозапуска
- **iptables-legacy** - обеспечение совместимости с Debian 13/13+ (использует nftables по умолчанию, требуется переключение перед UFW)

## 🔒 Безопасность

### SSH Hardening
```bash
# /etc/ssh/sshd_config
Port 22
PermitRootLogin no
PasswordAuthentication no
PubkeyAuthentication yes
MaxAuthTries 3
ClientAliveInterval 300
```

### Fail2Ban
- **SSH защита** - блокировка брутфорса
- **настроенные jail'ы** - оптимальные параметры
- **логирование** - детальная статистика

### UFW Firewall
```bash
# Стандартные правила
ufw default deny incoming
ufw default allow outgoing
ufw allow 22/tcp        # SSH
ufw allow 80/tcp        # HTTP
ufw allow 443/tcp       # HTTPS
ufw enable --force
```

**Важно для Debian 13+:** Скрипт автоматически переключает iptables на legacy режим перед установкой UFW, чтобы избежать конфликтов с nftables. Это критично для правильной работы файрвола.

## ⚡ Оптимизация производительности

### Сетевые настройки
```bash
# BBR congestion control
net.ipv4.tcp_congestion_control = bbr
net.core.default_qdisc = fq

# Увеличенные буферы
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
```

### Файловая система
```bash
# Увеличенные лимиты файлов
fs.file-max = 2097152
fs.inotify.max_user_watches = 524288

# Оптимизация памяти
vm.swappiness = 10
vm.vfs_cache_pressure = 50
```

### I/O планировщики
```bash
# SSD оптимизация
ACTION=="add|change", KERNEL=="sd[a-z]", ATTR{queue/rotational}=="0", ATTR{queue/scheduler}="mq-deadline"
# NVMe оптимизация
ACTION=="add|change", KERNEL=="nvme[0-9]n[0-9]", ATTR{queue/rotational}=="0", ATTR{queue/scheduler}="none"
```

## 🛠️ Утилиты скрипта

### vps-monitor.sh
Мониторинг системы:
```bash
vps-monitor.sh
```
- Системная информация
- Использование CPU/памяти/диска
- Сетевая статистика
- Top процессы

### vps-cleanup.sh
Очистка системы:
```bash
vps-cleanup.sh
```
- Очистка кэша пакетов
- Удаление старых логов
- Очистка Docker
- Удаление временных файлов

### vps-info.sh
Информация о системе:
```bash
vps-info.sh
```
- Детальная информация о железе
- Версия ОС и ядра
- Сетевые настройки
- Установленные пакеты

### network-test.sh
Тестирование сети:
```bash
network-test.sh
```
- Скорость соединения
- Пинг до популярных сервисов
- DNS тестирование
- Тестирование BBR

## 🔧 Конфигурационные файлы

### /etc/sysctl.d/99-custom.conf
```bash
# BBR
net.ipv4.tcp_congestion_control = bbr
net.core.default_qdisc = fq

# Память
vm.swappiness = 10
vm.vfs_cache_pressure = 50

# Файлы
fs.file-max = 2097152
fs.inotify.max_user_watches = 524288

# Сеть
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
```

### /etc/ssh/sshd_config
```bash
Port 22
PermitRootLogin no
PasswordAuthentication no
PubkeyAuthentication yes
MaxAuthTries 3
ClientAliveInterval 300
```

## 🔄 Обновление и поддержка

### Автоматические обновления
- Безопасные обновления включены
- Еженедельная очистка системы
- Автоматическое удаление старых ядер
- Мониторинг безопасности

### Ручное обновление
```bash
# Обновить скрипт
wget -O optimize_vps.sh https://raw.githubusercontent.com/snaplyze/optimize-linux/refs/heads/main/optimize_vps.sh

# Проверить систему
vps-monitor.sh

# Очистить систему
vps-cleanup.sh
```

## 🐛 Устранение проблем

### Проблемы с ядром
```bash
# Проверить версию ядра
uname -r

# Список установленных ядер
dpkg --list | grep linux-image

# Удалить старые ядра
sudo apt autoremove --purge
```

### Проблемы с SSH
```bash
# Проверить статус
sudo systemctl status ssh

# Проверить конфигурацию
sudo sshd -t

# Перезапустить сервис
sudo systemctl restart ssh
```

### Проблемы с Docker
```bash
# Проверить статус
sudo systemctl status docker

# Проверить группу
groups $USER

# Добавить в группу
sudo usermod -aG docker $USER
newgrp docker
```

## 📊 Мониторинг

### Системные метрики
- **CPU** - использование, load average
- **Память** - RAM, swap, кэши
- **Диск** - использование, I/O статистика
- **Сеть** - трафик, соединения, BBR

### Логирование
- **Системные логи** - journalctl
- **SSH логи** - auth.log
- **Docker логи** - container logs
- **Производительность** - sar, iostat

## 📈 Сравнение с стандартной установкой

| Характеристика | Стандартная установка | После оптимизации |
|---------------|---------------------|-------------------|
| Ядро | Стандартное | XanMod |
| Shell | Bash | Zsh + Starship |
| Безопасность | Базовая | Усиленная |
| Производительность | Стандартная | Оптимизированная |
| Контейнеризация | Нет | Docker CE |
| Мониторинг | Базовый | Расширенный |

## 📝 Лог изменений

### v2.0.0 (Текущая)
- ✨ **Go** установка (последняя версия, SHA256 верификация)
- ✨ **Node.js + NVM** с LTS версией
- ✨ Автоматическая инициализация Go и NVM в .zshrc
- ✨ npm install работает БЕЗ sudo для пользователя
- ✨ Исправлены все известные ошибки (wsl.conf, Docker, NVIDIA)
- ✨ Упрощенная docker daemon.json конфигурация
- ✨ iptables-legacy конфигурация для Debian 13 совместимости
- ✨ Системd socket activation для Docker
- ✨ Очистка стальных процессов перед запуском Docker
- ✨ Улучшенная документация

### v1.0.0 (Архив)
- 🐧 Базовый VPS скрипт оптимизации
- 🐧 XanMod kernel поддержка
- 🐧 Zsh + Starship конфигурация
- 🐧 Docker CE + Docker Compose
- 🐧 SSH hardening и безопасность
- 🐧 Системная оптимизация
- 🐧 Утилиты мониторинга

## 🤝 Сообщество

- **GitHub Issues** - сообщения об ошибках
- **GitHub Discussions** - вопросы и предложения
- **Pull Requests** - вклад в разработку

## 📄 Лицензия

MIT License - свободное использование и модификация.

---

**Создано с ❤️ для VPS сообщества**
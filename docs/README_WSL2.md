# WSL2 Optimization Script for Debian 12/13

Современный скрипт оптимизации WSL2 для Debian 12/13 (Bookworm/Trixie) с Zsh + Starship, заменяющий старую версию с Fish shell.

**🆕 Версия 2.0.0** - Полное переписание с нуля, замена Fish на Zsh, добавление WSL2-специфичных функций и современных оптимизаций.

## 🚀 Ключевые особенности

### 🐧 WSL2-специфичная оптимизация
- **systemd поддержка** - полная интеграция с systemd
- **wsl.conf конфигурация** - оптимизированные настройки WSL2
- **Windows интеграция** - функции для работы с Windows приложениями
- **Производительность** - тюнинг памяти, сети, файловой системы

### 🛠️ Современный Shell (Zsh + Starship)
- **Zsh** вместо Fish shell - более мощный и настраиваемый
- **Starship prompt** - красивый и информативный промпт
- **Плагины Zsh** - автодополнения, подсветка синтаксиса, подсказки
- **WSL2 функции** - `explorer()`, `cmd()`, `powershell()`, `wslperf()`

### 🐳 Контейнеризация
- **Docker CE** - последняя версия с Docker Compose
- **NVIDIA Container Toolkit** - поддержка GPU в контейнерах
- **CUDA Toolkit** - опциональная установка для разработки

### ⚡ Производительность
- **Оптимизация sysctl** - BBR, лимиты, сетевые настройки
- **I/O планировщики** - оптимизация для SSD/NVMe
- **Управление памятью** - swappiness, кэширование

## 📋 Системные требования

- **WSL2** с Windows 10/11
- **Debian 12/13** (Bookworm/Trixie)
- **Права sudo** для установки
- **Минимум 4GB RAM** (рекомендуется 8GB+)

## 🛠️ Установка и использование

### 1. Подготовка Windows

Создайте файл `%USERPROFILE%\.wslconfig`:

```ini
[wsl2]
memory=8GB
processors=4
autoMemoryReclaim=dropCache
sparseVhd=true
swap=2GB
dnsTunneling=true
networkingMode=mirrored
guiApplications=true

[experimental]
autoMemoryReclaim=dropCache
sparseVhd=true
```

### 2. Обновление WSL

В PowerShell (от имени администратора):

```powershell
wsl --update
wsl --set-default-version 2
```

### 3. Запуск скрипта

```bash
# Быстрый запуск (bash + curl)
bash <(curl -s https://raw.githubusercontent.com/snaplyze/optimize-linux/refs/heads/main/optimize_wsl2.sh)

# Или скачайте скрипт
wget https://raw.githubusercontent.com/snaplyze/optimize-linux/refs/heads/main/optimize_wsl2.sh

# Сделайте исполняемым
chmod +x optimize_wsl2.sh

# Запустите от имени root
sudo ./optimize_wsl2.sh
```

## 🎯 Конфигурация меню

Скрипт предлагает интерактивное меню с выбором компонентов:

### Системная конфигурация
- ✅ WSL2 конфигурация (wsl.conf + systemd)
- ✅ Обновление системы
- ✅ Базовые утилиты (git, curl, htop, fzf, bat)
- ✅ Создание пользователя с sudo
- ✅ Настройка локалей (ru_RU, en_US)
- ✅ Настройка часового пояса

### Разработка
- ✅ Zsh + Starship (замена Fish)
- ✅ SSH agent конфигурация

### Контейнеризация
- ✅ Docker CE + Docker Compose
- ✅ NVIDIA Container Toolkit + CUDA

### Оптимизация
- ✅ Автоматические обновления безопасности
- ✅ Производительность (sysctl, лимиты, I/O)

## 🎨 Zsh + Starship особенности

### Zsh плагины
- **zsh-autosuggestions** - подсказки команд как в Fish
- **zsh-syntax-highlighting** - подсветка синтаксиса
- **zsh-completions** - расширенные автодополнения

### Starship prompt
- 🌱 Git статус и ветка
- 🐋 Docker контекст
- 🐍 Python, ⬢ Node.js, 🐹 Go, 🦀 Rust версии
- ⏰ Время выполнения команд
- 💾 Использование памяти

### WSL2-специфичные функции
```bash
# Открыть папку в Windows Explorer
explorer /path/to/folder

# Выполнить Windows команду
cmd dir
powershell Get-Process

# Мониторинг производительности WSL2
wslperf
```

### Алиасы
```bash
# Docker
d='docker'
dc='docker compose'
dps='docker ps'
dcl='docker compose logs -f'

# Git
g='git'
gs='git status'
gcm='git commit -m'
gl='git log --oneline --graph'

# Система
ll='ls -alFh'
update='sudo apt update && sudo apt upgrade -y'
meminfo='free -h'
```

## 🐳 Docker и NVIDIA

### Docker оптимизация
- **overlay2 storage driver** - максимальная производительность
- **логирование** - ограничение размера логов
- **пулы адресов** - оптимизация сети
- **BuildKit** - ускорение сборки

### NVIDIA поддержка
- **GPU доступность** - проверка /dev/dxg
- **Container Toolkit** - интеграция с Docker
- **CUDA Toolkit** - опциональная установка
- **Автоопределение** - поддержка различных GPU

## ⚡ Оптимизация производительности

### Сетевые настройки
```bash
# BBR congestion control
net.ipv4.tcp_congestion_control = bbr
net.core.default_qdisc = fq

# Увеличенные буферы
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
```

### Файловая система
```bash
# Увеличенные лимиты файлов
fs.file-max = 2097152
fs.inotify.max_user_watches = 524288

# Оптимизация памяти
vm.swappiness = 10
vm.vfs_cache_pressure = 50
```

### I/O планировщики
```bash
# SSD оптимизация
ACTION=="add|change", KERNEL=="sd[a-z]", ATTR{queue/rotational}=="0", ATTR{queue/scheduler}="mq-deadline"
# NVMe оптимизация
ACTION=="add|change", KERNEL=="nvme[0-9]n[0-9]", ATTR{queue/rotational}=="0", ATTR{queue/scheduler}="none"
```

## 🛠️ Утилиты скрипта

### wsl2-monitor.sh
Мониторинг системы WSL2:
```bash
wsl2-monitor.sh
```
- Системная информация
- Использование CPU/памяти/диска
- WSL2 специфичная информация
- Top процессы

### wsl2-cleanup.sh
Очистка системы:
```bash
wsl2-cleanup.sh
```
- Очистка кэша пакетов
- Удаление старых логов
- Очистка Docker
- Удаление временных файлов

### wsl2-backup.sh
Резервное копирование:
```bash
wsl2-backup.sh [username]
```
- Конфигурационные файлы
- Настройки пользователей
- Сохранение в Windows

## 🔧 Конфигурационные файлы

### /etc/wsl.conf
```ini
[boot]
systemd=true
command="service docker start"

[automount]
enabled=true
options="metadata,uid=1000,gid=1000,umask=022,fmask=111"
root=/

[network]
generateHosts=true
hostname=debian-wsl

[interop]
enabled=true
appendWindowsPath=true

[user]
default=username

[filesystem]
case=sensitive
```

### /etc/docker/daemon.json
```json
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "storage-driver": "overlay2",
  "default-address-pools": [
    {
      "base": "172.17.0.0/12",
      "size": 24
    }
  ],
  "default-ulimits": {
    "nofile": 65536,
    "nproc": 32768
  },
  "features": {
    "buildkit": true
  }
}
```

## 🔄 Обновление и поддержка

### Автоматические обновления
- Безопасные обновления включены
- Еженедельная очистка системы
- Автоматическое удаление старых ядер

### Ручное обновление
```bash
# Обновить скрипт
wget -O optimize_wsl2.sh https://raw.githubusercontent.com/snaplyze/optimize-linux/refs/heads/main/optimize_wsl2.sh

# Проверить систему
wsl2-monitor.sh

# Очистить систему
wsl2-cleanup.sh
```

## 🐛 Устранение проблем

### Проблемы с systemd
```bash
# Проверить статус
systemctl status

# Перезапустить сервисы
sudo systemctl daemon-reload
sudo systemctl restart docker
```

### Проблемы с Docker
```bash
# Проверить группу
groups $USER

# Добавить в группу
sudo usermod -aG docker $USER

# Перезайти в систему
newgrp docker
```

### Проблемы с GPU
```bash
# Проверить доступность GPU
ls -la /dev/dxg
nvidia-smi

# Переустановить драйверы Windows
wsl --shutdown
```

## 📈 Сравнение с версией Fish

| Характеристика | Старая версия (Fish) | Новая версия (Zsh) |
|---------------|---------------------|-------------------|
| Shell | Fish | Zsh |
| Prompt | Starship | Starship (улучшенный) |
| Плагины | Fisher | Zsh нативные |
| Автодополнения | Базовые | Расширенные |
| WSL2 интеграция | Базовая | Полная |
| systemd поддержка | Ограниченная | Полная |
| Производительность | Стандартная | Оптимизированная |
| NVIDIA поддержка | Базовая | Расширенная |

## 📝 Лог изменений

### v2.0.0 (Текущая)
- ✨ Замена Fish shell на Zsh + Starship
- ✨ Полная поддержка systemd
- ✨ WSL2-специфичные функции
- ✨ Улучшенная производительность
- ✨ Расширенная поддержка NVIDIA/CUDA
- ✨ Интерактивное меню конфигурации
- ✨ Автоматические обновления безопасности

### v1.0.0 (Старая)
- 🐧 Fish shell + Fisher
- 🐧 Базовая WSL2 поддержка
- 🐧 Docker + NVIDIA Container Toolkit
- 🐧 CUDA Toolkit опционально

## 🤝 Сообщество

- **GitHub Issues** - сообщения об ошибках
- **GitHub Discussions** - вопросы и предложения
- **Pull Requests** - вклад в разработку

## 📄 Лицензия

MIT License - свободное использование и модификация.

---

**Создано с ❤️ для WSL2 сообщества**